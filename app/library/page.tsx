import Placeholder from '@/components/Placeholder';

export default function LibraryPage() {
  return (
    <Placeholder
      eyebrow="Library"
      title="Your material"
      body="Uploads, favorites, and previously framed exports in one place. Drop in your own images and they flow through Frame Studio exactly like museum works."
    />
  );
}
