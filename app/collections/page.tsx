import Placeholder from '@/components/Placeholder';

export default function CollectionsPage() {
  return (
    <Placeholder
      eyebrow="Collections"
      title="Your exhibitions"
      body="Group works into rotating sets — “Living Room”, “Brazilian Modernism”, “Japanese Landscapes” — each with its own cover, order, and rotation schedule. Connect a Supabase project to save collections (see docs/ARCHITECTURE.md)."
    />
  );
}
